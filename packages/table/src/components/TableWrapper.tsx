import React, { } from 'react';

export interface TableWrapperProps {

}

const TableWrapper: React.FC<TableWrapperProps> = (props) => {

  
  return (
    <div className="testTableWrapper"></div>
  );
};

export default TableWrapper;